<input type="checkbox" id="sidebar-opener" style="display: none;">
<aside id="sidebar">
  <label for="sidebar-opener" class="sidebar-toggle" title="Expand Sidebar"><i class="toggle icon"></i></label>
  <div class="sidebar-top">
    <p><%= __('sidebar.follow') %>:</p>
    <ul class="social-links">
      <% for (var i in theme.social_links){ %>
        <li><a class="social-tooltip" title="<%= i %>" href="<%- url_for(theme.social_links[i]) %>" target="_blank"><i id="icon-<%= i %>" class="icon"></i></a></li>
      <% } %>
    </ul>
  </div>
  <% if(is_post()) { %>
    <%- partial('post/nav', {post: page}) %>
  <% } %>
  <div class="widgets-container">
    <% if (theme.widgets) {
      theme.widgets.forEach(function(widget){ %>
      <%- partial('_widget/' + widget) %>
    <% })
    } %>
  </div>
</aside>